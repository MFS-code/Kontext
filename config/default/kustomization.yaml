apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../crd
  - ../rbac
  - ../manager

namespace: kontext-system

images:
  - name: controller
    newName: kontext-operator
    newTag: dev
